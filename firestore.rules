rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles temporaires très permissives pour débuggage
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
